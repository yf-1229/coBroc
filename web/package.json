{
  "name": "cobroc-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "wasm": "emcmake cmake -B build-emscripten -S . && emmake cmake --build build-emscripten",
    "e2e": "npm run build && node e2e/run_e2e.mjs",
    "e2e:serve": "node e2e/run_e2e.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
